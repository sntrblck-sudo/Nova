import { Command } from 'commander';
import { C as ClankerTokenV4 } from '../../clankerTokenV4-NqiH_j7p.js';
import 'viem';
import 'zod/v4';

declare function computeTicksFromMarketCap(startingMcap: number, pairedTokenValue: string): {
    tickLower: number;
    tickUpper: number;
};
declare function buildV4Config(f: Record<string, unknown>): ClankerTokenV4;
declare function resolveChainId(name: string): number;
declare function registerDeployCommand(program: Command): Command;

export { buildV4Config, computeTicksFromMarketCap, registerDeployCommand, resolveChainId };
