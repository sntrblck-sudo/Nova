import type { ChainId } from '../chain/chains.js';
import { BaseCurrency, type CurrencyMetadata } from '../currency/currency.js';
import type { Token } from './token.js';
export declare abstract class Native<TChainId extends ChainId = ChainId, TMetadata extends CurrencyMetadata = Record<string, unknown>> extends BaseCurrency<TChainId, TMetadata, 'token' | 'native'> {
    readonly type = "native";
    readonly isNative = true;
    readonly isToken = false;
    get id(): `${TChainId}:NATIVE`;
    abstract wrap(): Token<TChainId, string, TMetadata>;
}
//# sourceMappingURL=native.d.ts.map