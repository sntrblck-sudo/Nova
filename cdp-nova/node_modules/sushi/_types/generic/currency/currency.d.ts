import type { ChainId } from '../chain/chains.js';
import type { ID } from '../types/id.js';
import type { Native } from './native.js';
import type { SerializedCurrency } from './serialized-currency.js';
import type { Token } from './token.js';
export type CurrencyMetadata = Record<string, unknown>;
export declare abstract class BaseCurrency<TChainId extends ChainId = ChainId, TMetadata extends CurrencyMetadata = Record<string, unknown>, TCurrencyType extends string = string, TSerializedCurrency extends object = SerializedCurrency<TMetadata>> {
    readonly chainId: TChainId;
    readonly symbol: string;
    readonly name: string;
    readonly decimals: number;
    readonly metadata: TMetadata;
    abstract readonly type: TCurrencyType;
    abstract readonly isNative: boolean;
    abstract readonly isToken: boolean;
    constructor({ chainId, decimals, name, symbol, metadata, }: {
        chainId: TChainId;
        symbol: string;
        name: string;
        decimals: number;
        metadata?: TMetadata;
    });
    abstract get id(): ID<TChainId, string, true>;
    isSame(other: BaseCurrency<ChainId, CurrencyMetadata, string, object>): boolean;
    abstract wrap(): BaseCurrency<TChainId, TMetadata, TCurrencyType, TSerializedCurrency>;
    abstract toJSON(): TSerializedCurrency;
}
export type Currency<TChainId extends ChainId = ChainId, TMetadata extends CurrencyMetadata = Record<string, unknown>> = Native<TChainId> | Token<TChainId, string, TMetadata>;
//# sourceMappingURL=currency.d.ts.map