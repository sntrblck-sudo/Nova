import type { ChainId } from '../chain/chains.js';
import type { ID } from '../types/id.js';
import { BaseCurrency, type CurrencyMetadata } from './currency.js';
export declare abstract class Token<TChainId extends ChainId = ChainId, TAddress extends string = string, TMetadata extends CurrencyMetadata = Record<string, unknown>> extends BaseCurrency<TChainId, TMetadata, 'token'> {
    readonly type = "token";
    readonly isNative = false;
    readonly isToken = true;
    readonly address: TAddress;
    constructor({ address, ...rest }: {
        address: TAddress;
    } & ConstructorParameters<typeof BaseCurrency<TChainId, TMetadata>>[0]);
    get id(): ID<TChainId, TAddress>;
    isSame(other: BaseCurrency<ChainId, CurrencyMetadata, string, object>): boolean;
    abstract wrap(): Token<TChainId, TAddress, TMetadata>;
}
//# sourceMappingURL=token.d.ts.map