import type { EvmChainId } from '../../../chain/chains.js';
import { type SushiSwapV3FeeAmount } from '../../../config/index.js';
import type { EvmAddress, EvmToken } from '../../../currency/token.js';
type ComputeSushiSwapV3PoolAddressParams = {
    factoryAddress: EvmAddress;
    fee: SushiSwapV3FeeAmount;
    initCodeHashManualOverride?: EvmAddress | undefined;
} & ({
    tokenA: EvmToken;
    tokenB: EvmToken;
} | {
    tokenA: EvmAddress;
    tokenB: EvmAddress;
    chainId: EvmChainId;
});
/**
 * Computes a pool address
 * @param factoryAddress The Uniswap V3 factory address
 * @param tokenA The first token of the pair, irrespective of sort order
 * @param tokenB The second token of the pair, irrespective of sort order
 * @param fee The fee tier of the pool
 * @param initCodeHashManualOverride Override the init code hash used to compute the pool address if necessary
 * @returns The pool address
 */
export declare function computeSushiSwapV3PoolAddress(params: ComputeSushiSwapV3PoolAddressParams): EvmAddress;
export {};
//# sourceMappingURL=computePoolAddress.d.ts.map