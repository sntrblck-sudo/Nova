import { type Address } from 'viem';
import type { Percent } from '../../../../generic/math/percent.js';
import type { EvmToken } from '../../../currency/token.js';
export interface FeeOptions {
    /**
     * The percent of the output that will be taken as a fee.
     */
    fee: Percent;
    /**
     * The recipient of the fee.
     */
    recipient: Address;
}
export declare abstract class Payments {
    /**
     * Cannot be constructed.
     */
    private constructor();
    private static encodeFeeBips;
    static encodeUnwrapWETH9(amountMinimum: bigint, recipient: Address, feeOptions?: FeeOptions): `0x${string}`;
    static encodeSweepToken(token: EvmToken, amountMinimum: bigint, recipient: Address, feeOptions?: FeeOptions): `0x${string}`;
    static encodeRefundETH(): `0x${string}`;
}
//# sourceMappingURL=Payments.d.ts.map