import type { Address } from 'viem';
import type { EvmChainId } from '../../chain/index.js';
import { EvmToken, type EvmTokenOrigin } from '../../currency/token.js';
export declare function addressMapToTokenMap<ChainId extends EvmChainId>({ decimals, symbol, name, origin, }: {
    decimals: number;
    symbol: string;
    name: string;
    origin?: EvmTokenOrigin;
}, map: Record<ChainId, Address>): Record<ChainId, EvmToken>;
//# sourceMappingURL=address-map-to-token-map.d.ts.map