import type { Amount } from '../../generic/index.js';
import type { EvmToken } from '../currency/token.js';
import type { Fee } from './fee.js';
export declare enum PoolType {
    Wrap = "Wrap",
    Bridge = "Bridge",
    Classic = "Classic",
    Concentrated = "Concentrated",
    Stable = "Stable",
    Curve = "Curve",
    Unknown = "Unknown"
}
export declare abstract class Pool {
    abstract readonly liquidityToken: EvmToken;
    abstract readonly swapGasCost: bigint;
    abstract readonly minLiquidity: bigint;
    abstract get chainId(): number;
    abstract get fee(): Fee;
    abstract get assets(): EvmToken[];
    abstract get reserves(): Amount<EvmToken>[];
    abstract getLiquidityMinted(totalSupply: Amount<EvmToken>, tokenAmountA: Amount<EvmToken>, tokenAmountB: Amount<EvmToken>): Amount<EvmToken>;
    abstract getLiquidityValue(token: EvmToken, totalSupply: Amount<EvmToken>, liquidity: Amount<EvmToken>): Amount<EvmToken>;
    abstract involvesToken(token: EvmToken): boolean;
}
//# sourceMappingURL=pool.d.ts.map