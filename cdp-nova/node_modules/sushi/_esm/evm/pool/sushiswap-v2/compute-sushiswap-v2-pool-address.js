import invariant from 'tiny-invariant';
import { encodePacked, keccak256 } from 'viem/utils';
import { SUSHISWAP_V2_INIT_CODE_HASH, } from '../../config/index.js';
import { getCreate2Address } from '../../utils/get-create-2-address.js';
/**
 * Computes a pair address
 * @param factoryAddress The Uniswap V2 factory address
 * @param tokenA The first token of the pair, irrespective of sort order
 * @param tokenB The second token of the pair, irrespective of sort order
 * @param initCodeHashManualOverride Override the init code hash used to compute the pool address if necessary
 * @returns The pair address
 */
export const computeSushiSwapV2PoolAddress = ({ factoryAddress, tokenA, tokenB, initCodeHashManualOverride, }) => {
    const [token0, token1] = tokenA.sortsBefore(tokenB)
        ? [tokenA, tokenB]
        : [tokenB, tokenA]; // does safety checks
    invariant(token0.chainId === token1.chainId, 'CHAIN_ID');
    return getCreate2Address({
        from: factoryAddress,
        salt: keccak256(encodePacked(['address', 'address'], [token0.address, token1.address])),
        bytecodeHash: initCodeHashManualOverride ??
            SUSHISWAP_V2_INIT_CODE_HASH[token0.chainId],
        chainId: token0.chainId,
    }).toLowerCase();
};
//# sourceMappingURL=compute-sushiswap-v2-pool-address.js.map