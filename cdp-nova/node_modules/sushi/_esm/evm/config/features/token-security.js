import { EvmChainId } from '../../chain/index.js';
const SUPPORTED_CHAIN_IDS = [
    EvmChainId.AVALANCHE,
    EvmChainId.BSC,
    EvmChainId.FANTOM,
    EvmChainId.GNOSIS,
    EvmChainId.HARMONY,
    EvmChainId.ETHEREUM,
    EvmChainId.ARBITRUM,
    EvmChainId.OPTIMISM,
    EvmChainId.ZKSYNC_ERA,
    EvmChainId.LINEA,
    EvmChainId.BASE,
    EvmChainId.POLYGON,
    EvmChainId.SCROLL,
    EvmChainId.BLAST,
    EvmChainId.CRONOS,
    EvmChainId.MANTLE,
    EvmChainId.MANTA,
    EvmChainId.ZKLINK,
];
export const TokenSecurityChainIds = SUPPORTED_CHAIN_IDS;
export const isTokenSecurityChainId = (chainId) => SUPPORTED_CHAIN_IDS.includes(chainId);
//# sourceMappingURL=token-security.js.map