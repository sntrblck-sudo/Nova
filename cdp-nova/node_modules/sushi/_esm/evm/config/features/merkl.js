import { EvmChainId } from '../../chain/index.js';
export const MERKL_SUPPORTED_CHAIN_IDS = [
    EvmChainId.ETHEREUM,
    EvmChainId.OPTIMISM,
    EvmChainId.BASE,
    EvmChainId.BSC,
    EvmChainId.GNOSIS,
    EvmChainId.POLYGON,
    EvmChainId.ARBITRUM,
    EvmChainId.AVALANCHE,
    EvmChainId.THUNDERCORE,
    EvmChainId.CORE,
    EvmChainId.BLAST,
    EvmChainId.SCROLL,
    EvmChainId.LINEA,
    EvmChainId.SKALE_EUROPA,
    EvmChainId.ROOTSTOCK,
    EvmChainId.KATANA,
    EvmChainId.HEMI,
];
export const MerklChainIds = MERKL_SUPPORTED_CHAIN_IDS;
export const isMerklChainId = (chainId) => MERKL_SUPPORTED_CHAIN_IDS.includes(chainId);
//# sourceMappingURL=merkl.js.map