import { TvmToken, } from '../../currency/token.js';
export function addressMapToTokenMap({ decimals, symbol, name, origin, }, map) {
    return Object.fromEntries(Object.entries(map).map(([chainId, address]) => [
        Number(chainId),
        new TvmToken({
            chainId: Number(chainId),
            address: address,
            decimals,
            symbol,
            name,
            origin,
        }),
    ]));
}
//# sourceMappingURL=address-map-to-token-map.js.map