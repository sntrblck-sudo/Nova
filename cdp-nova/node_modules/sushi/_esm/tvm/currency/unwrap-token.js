import { WNATIVE } from '../config/tokens/wrapped-native.js';
export function unwrapTvmToken(currency) {
    if (currency.type === 'native') {
        return currency;
    }
    if (currency.address.toLowerCase() ===
        WNATIVE[currency.chainId].address.toLowerCase()) {
        return WNATIVE[currency.chainId];
    }
    return currency;
}
//# sourceMappingURL=unwrap-token.js.map