import { BaseCurrency } from '../currency/currency.js';
export class Native extends BaseCurrency {
    constructor() {
        super(...arguments);
        this.type = 'native';
        this.isNative = true;
        this.isToken = false;
    }
    get id() {
        return `${this.chainId}:NATIVE`;
    }
}
//# sourceMappingURL=native.js.map