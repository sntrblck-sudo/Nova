import { Address } from "viem";
import { Uploader, UploadResult } from "../types";
/**
 * Zora IPFS uploader implementation
 */
export declare class ZoraUploader implements Uploader {
    constructor(creatorAddress: Address);
    private creatorAddress;
    private jwtApiKey;
    private jwtApiKeyExpiresAt;
    getJWTApiKey(): Promise<string>;
    upload(file: File): Promise<UploadResult>;
}
/**
 * Create a new Zora IPFS uploader
 */
export declare function createZoraUploaderForCreator(creatorAddress: Address): Uploader;
//# sourceMappingURL=zora.d.ts.map