import { jsx, jsxs, Fragment } from "react/jsx-runtime";
import { useState, useEffect, useCallback } from "react";
import { Toast } from "../../internal/components/Toast.js";
import { SignatureIcon } from "./SignatureIcon.js";
import { SignatureLabel } from "./SignatureLabel.js";
import { useSignatureContext } from "./SignatureProvider.js";
const DEFAULT_CHILDREN = /* @__PURE__ */ jsxs(Fragment, { children: [
  /* @__PURE__ */ jsx(SignatureIcon, {}),
  /* @__PURE__ */ jsx(SignatureLabel, {})
] });
function SignatureToast({
  children = DEFAULT_CHILDREN,
  className,
  duration = 5e3,
  position = "bottom-center"
}) {
  const {
    lifecycleStatus
  } = useSignatureContext();
  const [isToastVisible, setIsToastVisible] = useState(false);
  useEffect(() => {
    if (lifecycleStatus.statusName !== "init") {
      setIsToastVisible(true);
    }
  }, [lifecycleStatus]);
  const closeToast = useCallback(() => {
    setIsToastVisible(false);
  }, []);
  return /* @__PURE__ */ jsx(Toast, { position, className, duration, open: isToastVisible, onClose: closeToast, children });
}
export {
  SignatureToast
};
//# sourceMappingURL=SignatureToast.js.map
