import { jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { Spinner } from "../../internal/components/Spinner.js";
import { ErrorSvg } from "../../internal/svg/errorSvg.js";
import { SuccessSvg } from "../../internal/svg/successSvg.js";
import { cn, text } from "../../styles/theme.js";
import { useSignatureContext } from "./SignatureProvider.js";
function SignatureIcon({
  className
}) {
  const {
    lifecycleStatus
  } = useSignatureContext();
  const icon = useMemo(() => {
    if (lifecycleStatus.statusName === "success") {
      return /* @__PURE__ */ jsx(SuccessSvg, {});
    }
    if (lifecycleStatus.statusName === "error") {
      return /* @__PURE__ */ jsx(ErrorSvg, {});
    }
    if (lifecycleStatus.statusName === "pending") {
      return /* @__PURE__ */ jsx(Spinner, { className: "ock:px-1.5 ock:py-1.5" });
    }
    return null;
  }, [lifecycleStatus.statusName]);
  if (!icon) {
    return null;
  }
  return /* @__PURE__ */ jsx("div", { className: cn(text.label2, className), "data-testid": "ockSignatureIcon", children: icon });
}
export {
  SignatureIcon
};
//# sourceMappingURL=SignatureIcon.js.map
