import { getOnchainKitConfig } from "../../core/OnchainKitConfig.js";
import { useIsInMiniApp } from "../hooks/useIsInMiniApp.js";
function IfInMiniApp({
  fallback,
  children
}) {
  const {
    isInMiniApp
  } = useIsInMiniApp();
  const miniKit = getOnchainKitConfig("miniKit");
  if ((miniKit == null ? void 0 : miniKit.enabled) && isInMiniApp) {
    return children;
  }
  return fallback || null;
}
export {
  IfInMiniApp
};
//# sourceMappingURL=IfInMiniApp.js.map
