'use client';
import { jsx } from "react/jsx-runtime";
import { cn, text } from "../../styles/theme.js";
import { getSwapMessage } from "../utils/getSwapMessage.js";
import { useSwapContext } from "./SwapProvider.js";
function SwapMessage({
  className,
  render
}) {
  const {
    address,
    to,
    from,
    lifecycleStatus
  } = useSwapContext();
  const message = getSwapMessage({
    address,
    from,
    lifecycleStatus,
    to
  });
  if (render) {
    return render({
      message: message || null
    });
  }
  return /* @__PURE__ */ jsx("div", { className: cn("ock:flex ock:h-7 ock:pt-2", text.label2, "ock:text-ock-foreground-muted", className), "data-testid": "ockSwapMessage_Message", children: message });
}
export {
  SwapMessage
};
//# sourceMappingURL=SwapMessage.js.map
