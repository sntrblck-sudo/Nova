import { jsx } from "react/jsx-runtime";
import { QuantitySelector } from "../../../internal/components/QuantitySelector.js";
import { useNFTContext } from "../NFTProvider.js";
import { useMintAnalytics } from "../../hooks/useMintAnalytics.js";
import { cn } from "../../../styles/theme.js";
function NFTQuantitySelector({
  className
}) {
  const {
    maxMintsPerWallet,
    setQuantity
  } = useNFTContext();
  const {
    handleQuantityChange
  } = useMintAnalytics();
  if (maxMintsPerWallet === 1) {
    return null;
  }
  const handleQuantityUpdate = (value) => {
    setQuantity(value);
    const quantity = Number.parseInt(value, 10);
    if (!Number.isNaN(quantity)) {
      handleQuantityChange(quantity);
    }
  };
  return /* @__PURE__ */ jsx("div", { className: cn("ock:py-1", className), children: /* @__PURE__ */ jsx(QuantitySelector, { className, onChange: handleQuantityUpdate, minQuantity: 1, maxQuantity: maxMintsPerWallet, placeholder: "" }) });
}
export {
  NFTQuantitySelector
};
//# sourceMappingURL=NFTQuantitySelector.js.map
