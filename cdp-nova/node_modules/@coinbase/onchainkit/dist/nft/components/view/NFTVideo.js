import { jsx } from "react/jsx-runtime";
import { useNFTContext } from "../NFTProvider.js";
import { useRef, useEffect, useCallback } from "react";
import { defaultNFTSvg } from "../../../internal/svg/defaultNFTSvg.js";
import { cn } from "../../../styles/theme.js";
function NFTVideo({
  className,
  square = true,
  onLoading,
  onLoaded,
  onError
}) {
  const {
    animationUrl,
    imageUrl
  } = useNFTContext();
  const videoRef = useRef(null);
  useEffect(() => {
    if (animationUrl && (videoRef == null ? void 0 : videoRef.current)) {
      videoRef.current.onloadstart = () => {
        onLoading == null ? void 0 : onLoading(animationUrl);
      };
      videoRef.current.onloadeddata = () => {
        onLoaded == null ? void 0 : onLoaded();
      };
      videoRef.current.onerror = (error) => {
        onError == null ? void 0 : onError({
          error: typeof error === "string" ? error : error.type,
          code: "NmNVc01",
          // NFT module NFTVideo component 01 error
          message: "Error loading video"
        });
      };
    }
  }, [animationUrl, onLoading, onLoaded, onError]);
  const handleClick = useCallback((e) => {
    e.stopPropagation();
  }, []);
  if (!animationUrl) {
    return /* @__PURE__ */ jsx("div", { className: "ock:max-h-350 ock:w-350 ock:max-w-350", children: defaultNFTSvg });
  }
  return /* @__PURE__ */ jsx("div", { className: cn("ock:grid ock:w-full", "ock:[&>*]:col-start-1 ock:[&>*]:col-end-1 ock:[&>*]:row-start-1 ock:[&>*]:row-end-1", {
    "content-center justify-center": !square
  }, {
    "aspect-square": square
  }, className), children: /* @__PURE__ */ jsx("video", { ref: videoRef, "data-testid": "ockNFTVideo", onClick: handleClick, className: cn({
    "h-full w-full object-cover aspect-square": square
  }), poster: imageUrl, controls: true, loop: true, src: animationUrl, muted: true, autoPlay: true, playsInline: true, draggable: false, width: "100%" }) });
}
export {
  NFTVideo
};
//# sourceMappingURL=NFTVideo.js.map
