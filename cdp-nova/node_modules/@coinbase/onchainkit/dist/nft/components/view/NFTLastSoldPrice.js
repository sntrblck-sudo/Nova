import { jsxs, jsx } from "react/jsx-runtime";
import { useNFTContext } from "../NFTProvider.js";
import { cn, text } from "../../../styles/theme.js";
import { formatToDecimalString } from "../../../internal/utils/formatter.js";
import { formatAmount } from "../../../token/utils/formatAmount.js";
function NFTLastSoldPrice({
  className,
  label = "Last sale price"
}) {
  const {
    lastSoldPrice
  } = useNFTContext();
  if (!(lastSoldPrice == null ? void 0 : lastSoldPrice.amount) || !(lastSoldPrice == null ? void 0 : lastSoldPrice.currency) || !(lastSoldPrice == null ? void 0 : lastSoldPrice.amountUSD)) {
    return null;
  }
  const {
    amount,
    currency,
    amountUSD
  } = lastSoldPrice;
  return /* @__PURE__ */ jsxs("div", { className: cn(text.label2, "ock:flex ock:justify-between", className), children: [
    /* @__PURE__ */ jsx("div", { className: cn("ock:text-ock-foreground-muted"), children: label }),
    /* @__PURE__ */ jsxs("div", { className: "ock:flex", children: [
      /* @__PURE__ */ jsxs("div", { className: text.label1, children: [
        formatToDecimalString(amount),
        " ",
        currency
      ] }),
      /* @__PURE__ */ jsx("div", { className: "ock:px-2", children: "~" }),
      /* @__PURE__ */ jsxs("div", { children: [
        "$",
        formatAmount(`${amountUSD}`, {
          minimumFractionDigits: 2,
          maximumFractionDigits: 2
        })
      ] })
    ] })
  ] });
}
export {
  NFTLastSoldPrice
};
//# sourceMappingURL=NFTLastSoldPrice.js.map
