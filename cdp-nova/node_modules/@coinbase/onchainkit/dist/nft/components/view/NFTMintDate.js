import { jsxs, jsx } from "react/jsx-runtime";
import { useNFTContext } from "../NFTProvider.js";
import { useMemo } from "react";
import { cn, text } from "../../../styles/theme.js";
const DATE_OPTIONS = {
  year: "numeric",
  month: "short",
  day: "numeric"
};
function NFTMintDate({
  className,
  label = "Mint date"
}) {
  const {
    mintDate
  } = useNFTContext();
  const formattedDate = useMemo(() => {
    if (!mintDate) {
      return null;
    }
    const formatter = new Intl.DateTimeFormat(void 0, DATE_OPTIONS);
    return formatter.format(mintDate);
  }, [mintDate]);
  if (!formattedDate) {
    return null;
  }
  return /* @__PURE__ */ jsxs("div", { className: cn(text.label2, "ock:flex ock:items-center ock:justify-between", className), children: [
    /* @__PURE__ */ jsx("div", { className: cn("ock:text-ock-foreground-muted"), children: label }),
    /* @__PURE__ */ jsx("div", { children: formattedDate })
  ] });
}
export {
  NFTMintDate
};
//# sourceMappingURL=NFTMintDate.js.map
