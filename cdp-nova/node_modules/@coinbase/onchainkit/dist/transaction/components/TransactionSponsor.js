import { jsx } from "react/jsx-runtime";
import { cn, text } from "../../styles/theme.js";
import { useTransactionContext } from "./TransactionProvider.js";
function TransactionSponsor({
  className
}) {
  const {
    errorMessage,
    lifecycleStatus,
    paymasterUrl,
    receipt,
    transactionHash,
    transactionId
  } = useTransactionContext();
  const transactionInProgress = transactionId || transactionHash;
  if (lifecycleStatus.statusName !== "init" || !paymasterUrl || errorMessage || transactionInProgress || receipt) {
    return null;
  }
  return /* @__PURE__ */ jsx("div", { className: cn(text.label2, "ock:flex", className), children: /* @__PURE__ */ jsx("p", { className: "text-ock-foreground-muted", children: "Zero transaction fee" }) });
}
export {
  TransactionSponsor
};
//# sourceMappingURL=TransactionSponsor.js.map
