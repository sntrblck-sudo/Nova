'use client';
import { jsx } from "react/jsx-runtime";
import { IdentityLayout } from "./IdentityLayout.js";
import { IdentityProvider } from "./IdentityProvider.js";
import { useOnchainKit } from "../../useOnchainKit.js";
import { useAccount } from "wagmi";
function Identity({
  address: addressProp,
  chain,
  children,
  className,
  hasCopyAddressOnClick,
  schemaId
}) {
  const {
    chain: contextChain
  } = useOnchainKit();
  const accountChain = chain ?? contextChain;
  const {
    address
  } = useAccount();
  if (!address && !addressProp) {
    return null;
  }
  return /* @__PURE__ */ jsx(IdentityProvider, { address: addressProp || address, schemaId, chain: accountChain, children: /* @__PURE__ */ jsx(IdentityLayout, { className, hasCopyAddressOnClick, children }) });
}
export {
  Identity
};
//# sourceMappingURL=Identity.js.map
