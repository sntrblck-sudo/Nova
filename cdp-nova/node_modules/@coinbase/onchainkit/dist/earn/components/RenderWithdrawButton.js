import { jsx } from "react/jsx-runtime";
import { Spinner } from "../../internal/components/Spinner.js";
import { prefixClassName, cn, text, pressable } from "../../styles/theme.js";
function RenderWithdrawButton({
  context,
  onSubmit,
  onSuccess,
  isDisabled,
  withdrawAmountError,
  withdrawnAmount,
  vaultToken
}) {
  const classNames = prefixClassName(cn(pressable.primary, "rounded-ock-default", "w-full rounded-xl", "px-4 py-3 font-medium leading-6", isDisabled && pressable.disabled, text.headline, "text-ock-foreground-inverse"));
  if (context.receipt) {
    return /* @__PURE__ */ jsx("button", { className: classNames, onClick: onSuccess, disabled: isDisabled, children: `Withdrew ${withdrawnAmount} ${vaultToken == null ? void 0 : vaultToken.symbol}` });
  }
  if (context.errorMessage || withdrawAmountError) {
    return /* @__PURE__ */ jsx("button", { className: classNames, onClick: onSubmit, disabled: isDisabled, children: withdrawAmountError ?? "Try again" });
  }
  if (context.isLoading) {
    return /* @__PURE__ */ jsx("button", { className: classNames, disabled: isDisabled, children: /* @__PURE__ */ jsx(Spinner, {}) });
  }
  return /* @__PURE__ */ jsx("button", { className: classNames, disabled: isDisabled, onClick: onSubmit, children: "Withdraw" });
}
export {
  RenderWithdrawButton
};
//# sourceMappingURL=RenderWithdrawButton.js.map
