'use client';
import { jsx } from "react/jsx-runtime";
import { useAmountInput } from "../../internal/hooks/useAmountInput.js";
import { FundCardPresetAmountInputItem } from "./FundCardPresetAmountInputItem.js";
import { useFundContext } from "./FundCardProvider.js";
function FundCardPresetAmountInputList() {
  const {
    presetAmountInputs,
    currency,
    selectedInputType,
    exchangeRate,
    setFundAmountFiat,
    setFundAmountCrypto
  } = useFundContext();
  const {
    handleFiatChange
  } = useAmountInput({
    setFiatAmount: setFundAmountFiat,
    setCryptoAmount: setFundAmountCrypto,
    selectedInputType,
    exchangeRate: String(exchangeRate)
  });
  if (!presetAmountInputs) {
    return null;
  }
  return /* @__PURE__ */ jsx("div", { "data-testid": "ockPresetAmountInputList", className: "ock:flex ock:w-full ock:flex-wrap ock:items-center ock:justify-between ock:gap-2 ock:pt-8", children: presetAmountInputs.map((amount, index) => /* @__PURE__ */ jsx(FundCardPresetAmountInputItem, { presetAmountInput: amount, onClick: handleFiatChange, currency }, index)) });
}
export {
  FundCardPresetAmountInputList
};
//# sourceMappingURL=FundCardPresetAmountInputList.js.map
