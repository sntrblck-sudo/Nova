'use client';
import { jsx } from "react/jsx-runtime";
import { cn, text } from "../../styles/theme.js";
import { isSwapError } from "../../swap/utils/isSwapError.js";
import { useBuyContext } from "./BuyProvider.js";
function BuyMessage() {
  var _a;
  const {
    lifecycleStatus
  } = useBuyContext();
  if (isSwapError(lifecycleStatus.statusData)) {
    const message = lifecycleStatus.statusData.message || "Something went wrong. Please try again.";
    const textColor = ((_a = lifecycleStatus == null ? void 0 : lifecycleStatus.statusData) == null ? void 0 : _a.code) === "TmBPc05" ? "text-ock-foreground-muted" : "text-ock-error";
    return /* @__PURE__ */ jsx("div", { className: cn(textColor, text.label2), children: message });
  }
  return null;
}
export {
  BuyMessage
};
//# sourceMappingURL=BuyMessage.js.map
