import { ReactNode } from 'react';
import { Chain } from 'wagmi/chains';
import { AppConfig } from './core/types';
import { MiniKitOptions } from './minikit/types';
import { PublicClient } from 'viem';
export type OnchainKitProviderReact = {
    analytics?: boolean;
    apiKey?: string;
    chain: Chain;
    children: ReactNode;
    config?: AppConfig;
    sessionId?: string;
    projectId?: string;
    rpcUrl?: string;
    miniKit?: MiniKitOptions;
    defaultPublicClients?: {
        [chainId: number]: PublicClient;
    };
};
/**
 * Provides the OnchainKit React Context to the app.
 */
export declare function OnchainKitProvider({ analytics, apiKey, chain, children, config, projectId, rpcUrl, miniKit, defaultPublicClients, }: OnchainKitProviderReact): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=OnchainKitProvider.d.ts.map