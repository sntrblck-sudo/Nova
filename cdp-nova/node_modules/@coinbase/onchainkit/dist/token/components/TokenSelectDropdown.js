'use client';
import { jsx } from "react/jsx-runtime";
import { useState, useRef, useCallback } from "react";
import { DropdownMenu, DropdownMenuItem } from "../../internal/components/DropdownMenu.js";
import { cn } from "../../styles/theme.js";
import { TokenRow } from "./TokenRow.js";
import { TokenSelectButton } from "./TokenSelectButton.js";
function TokenSelectDropdown({
  options,
  setToken,
  token
}) {
  const [isOpen, setIsOpen] = useState(false);
  const buttonRef = useRef(null);
  const closeDropdown = useCallback(() => {
    setIsOpen(false);
  }, []);
  const toggleDropdown = useCallback(() => {
    setIsOpen((prev) => !prev);
  }, []);
  return /* @__PURE__ */ jsx("div", { className: "ock:relative ock:max-w-fit ock:shrink-0", children: /* @__PURE__ */ jsx(DropdownMenu, { trigger: /* @__PURE__ */ jsx(TokenSelectButton, { ref: buttonRef, onClick: toggleDropdown, isOpen, token }), isOpen, onClose: closeDropdown, align: "end", "aria-label": "Select token", className: "ock:rounded-ock-default ock:text-ock-foreground ock:flex ock:max-h-80 ock:w-[200px] ock:flex-col ock:overflow-y-hidden ock:ock-scrollbar ock:overflow-y-auto", children: options.map((token2) => /* @__PURE__ */ jsx(TokenRow, { as: DropdownMenuItem, className: cn("ock:bg-ock-background-inverse", "ock:px-4 ock:py-2"), token: token2, onClick: () => {
    setToken(token2);
    setIsOpen(false);
  } }, token2.name + token2.address)) }) });
}
export {
  TokenSelectDropdown
};
//# sourceMappingURL=TokenSelectDropdown.js.map
