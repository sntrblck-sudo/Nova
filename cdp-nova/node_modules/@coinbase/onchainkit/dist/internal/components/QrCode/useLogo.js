import { jsxs, jsx } from "react/jsx-runtime";
import React, { useMemo } from "react";
import ReactDOMServer from "react-dom/server";
import { coinbaseWalletSvg } from "../../svg/coinbaseWalletSvg.js";
const defaultSvgString = ReactDOMServer.renderToString(coinbaseWalletSvg);
const defaultSvgDataUri = `data:image/svg+xml;charset=utf-8,${encodeURIComponent(defaultSvgString)}`;
function useLogo({
  size,
  logo = defaultSvgDataUri,
  logoSize,
  logoBackgroundColor,
  logoMargin,
  logoBorderRadius
}) {
  const svgLogo = useMemo(() => {
    const transformedLogo = React.isValidElement(logo) ? `data:image/svg+xml;charset=utf-8,${encodeURIComponent(ReactDOMServer.renderToString(logo))}` : logo;
    const logoPosition = (size - logoSize - logoMargin * 2) / 2;
    const logoBackgroundSize = logoSize + logoMargin * 2;
    return /* @__PURE__ */ jsxs("g", { transform: `translate(${logoPosition}, ${logoPosition})`, children: [
      /* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsx("clipPath", { id: "clip-logo", children: /* @__PURE__ */ jsx("rect", { width: logoSize, height: logoSize, rx: logoBorderRadius, ry: logoBorderRadius }) }) }),
      /* @__PURE__ */ jsx("g", { children: /* @__PURE__ */ jsx("rect", { width: logoBackgroundSize, height: logoBackgroundSize, rx: logoBorderRadius, ry: logoBorderRadius, fill: logoBackgroundColor }) }),
      /* @__PURE__ */ jsx("g", { transform: `translate(${logoMargin}, ${logoMargin})`, children: /* @__PURE__ */ jsx("image", { "data-testid": "qr-code-logo", width: logoSize, height: logoSize, preserveAspectRatio: "xMidYMid slice", href: String(transformedLogo), clipPath: "url(#clip-logo)" }) })
    ] });
  }, [logo, logoBackgroundColor, logoBorderRadius, logoMargin, logoSize, size]);
  return svgLogo;
}
export {
  useLogo
};
//# sourceMappingURL=useLogo.js.map
