import { jsxs, jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { CORNER_SIZE } from "./useDotsPath.js";
function useCorners(size, matrixLength, backgroundColor, fillColor, uid) {
  const dotSize = size / matrixLength;
  const rectSize = dotSize * CORNER_SIZE;
  const circleRadius = dotSize * 2;
  const circleStrokeWidth = dotSize + 1;
  const corners = useMemo(() => /* @__PURE__ */ jsxs("g", { children: [
    /* @__PURE__ */ jsx("rect", { x: 0, y: 0, rx: 9.5, ry: 9.5, width: rectSize, height: rectSize, fill: fillColor, id: `Corner-top-left-${uid}` }),
    /* @__PURE__ */ jsx("rect", { x: 0, y: size - rectSize, rx: 9.5, ry: 9.5, width: rectSize, height: rectSize, fill: fillColor, id: `Corner-bottom-left-${uid}` }),
    /* @__PURE__ */ jsx("rect", { x: size - rectSize, y: 0, rx: 9.5, ry: 9.5, width: rectSize, height: rectSize, fill: fillColor, id: `Corner-top-right-${uid}` }),
    /* @__PURE__ */ jsx("circle", { cx: rectSize / 2, cy: rectSize / 2, r: circleRadius, stroke: backgroundColor, strokeWidth: circleStrokeWidth, fill: "none" }),
    /* @__PURE__ */ jsx("circle", { cx: rectSize / 2, cy: size - rectSize / 2, r: circleRadius, stroke: backgroundColor, strokeWidth: circleStrokeWidth, fill: "none" }),
    /* @__PURE__ */ jsx("circle", { cx: size - rectSize / 2, cy: rectSize / 2, r: circleRadius, stroke: backgroundColor, strokeWidth: circleStrokeWidth, fill: "none" })
  ] }), [backgroundColor, circleRadius, circleStrokeWidth, fillColor, rectSize, size, uid]);
  return corners;
}
export {
  useCorners
};
//# sourceMappingURL=useCorners.js.map
