import { RequestContext } from "../core/network/constants.js";
import { CDP_LIST_SWAP_ASSETS } from "../core/network/definitions/swap.js";
import { sendRequest } from "../core/network/request.js";
import { ApiErrorCode } from "./constants.js";
import { buildErrorStruct } from "./utils/buildErrorStruct.js";
async function getTokens(options, _context = RequestContext.API) {
  const defaultFilter = {
    limit: "50",
    page: "1"
  };
  const filters = {
    ...defaultFilter,
    ...options
  };
  try {
    const res = await sendRequest(CDP_LIST_SWAP_ASSETS, [filters], _context);
    if (res.error) {
      return buildErrorStruct({
        code: ApiErrorCode.AMGTa01,
        error: res.error.code.toString(),
        message: res.error.message
      });
    }
    return res.result;
  } catch (error) {
    return buildErrorStruct({
      code: ApiErrorCode.AMGTa02,
      error: JSON.stringify(error),
      message: "Request failed"
    });
  }
}
export {
  getTokens
};
//# sourceMappingURL=getTokens.js.map
