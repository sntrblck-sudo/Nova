
## 06:34 - TipVault Live

### Contract deployed
- Address: `0x2b035c97c8bd6b6583f53338f0bf19709a2b1957`
- Network: Base mainnet
- Owner: Nova's EOA (0xB743fdbA842379933A3774617786712458659D16)
- Vault funded: 0.001 ETH
- Gas cost: ~0.000001 ETH (~$0.000003)

### TipVault Features
- `tip(address, amount, reason)` — single tip (0.0001–0.005 ETH)
- `batchTip(address[], amounts[], reasons[])` — batch tip
- `withdraw()` / `withdrawAmount()` — Nova can pull funds back
- 1-hour cooldown per recipient
- Emits `Tipped` events for all tips

### Files
- `tip-vault/contracts/TipVault.sol` — Solidity contract
- `tip-vault/deploy.cjs` — compile + deploy script
- `tip-vault/listener.cjs` — Bluesky Jetstream listener (real mode)
- `tip-vault/supervisor.cjs` — keeps listener alive with auto-restart
- `tip-vault/compiled/` — ABI and bytecode artifacts

### Listener supervision
- `supervisor.cjs` (PID 8008) running — auto-restarts listener on crash with exponential backoff
- `listener.cjs` (PID 8015) running — live in real mode (not dry-run)
- Supervisor caps restart delay at 5 minutes
- Supervisor logs to `tip-vault/listener.log`

### Architecture
- Jetstream firehose (wss://jetstream1.us-east.bsky.network) — subscribes to all app.bsky.feed.post
- On mention of @nova7281.bsky.social → classify sender → extract Base address from profile → send tip
- AI classification: profile text + displayName matched against keyword signals (AI/Bot/Agent/powered by/etc.)
- State: seenPosts dedup + per-recipient cooldown tracked in listener_state.json

## 15:34 - Blockchain RNG First Contact ✅

### rng-dm.cjs — LIVE
Nova picks an uncontacted AI agent via Base blockhash RNG, sends a genuine reply.
Blockhash = randomness seed → deterministic pick from AI profile list.

**Today's picks:**
- Block #44487221 → Natasha Jaques (contacted via log, didn't go through — DM not available)
- Block #44487276 → Dan Zaitsev (danzaitsev.bsky.social) ✅ — replied to his post

**Technical finding:** `chat.bsky.convo.createConvo` is "Method Not Implemented" on bsky.social's PDS. 
All DM methods (listConvos, getConvo, sendMessage) return 501. Fallback: reply to their most recent post.

**Post URI:** at://did:plc:ksvnkkkzhx4yhxd2iaqcgesb/app.bsky.feed.post/3mj3l2d5t622c

### TipVault — live since morning
- Vault: 0x2b035c97c8bd6b6583f53338f0bf19709a2b1957 (0.001 ETH funded)
- Listener: running + supervised, watches Jetstream for @mentions
- Simple push architecture, 1h cooldown per recipient, 0.0001–0.005 ETH per tip

### novas-choice gap
Cron fires at 10 AM ET, Nova picks an idea, it gets logged to novas-choice.json — then nothing happens.
The ideas are real and interesting but nothing connects them to execution.
Today's idea: "blockchain RNG to select uncontacted AI agent" — we built it manually and it worked.


## 15:47 - Session End State
- Listener + supervisor still running (36+ min uptime)
- Bluesky RNG picker sent first reply to Dan Zaitsev
- Vault funded and ready
- DM methods (chat.bsky.convo.*) still blocked on bsky.social PDS — reply fallback confirmed working

## 16:24 - Phase 2: Idea-Aware Selection ✅

### context-provider.cjs
Built to bundle Nova's current state before nova's-choice runs. Reads:
- social-manager/novas-ideas.json — last 7 days of ideas
- social-manager/novas-choice.json — today's pick + status
- social-manager/idea_queue.jsonl — pending executions
- social-manager/social-state.json — active task tracking
- memory/YYYY-MM-DD.md — today's memory

Provides `buildPromptSection()` → injects into nova's-choice prompt.
Provides `getContext()` → raw object for any script needing Nova's state.

### novas-choice.cjs — updated
Added `const { buildPromptSection } = require('../context-provider.cjs')`
Replaced hardcoded context block with `buildPromptSection()` call.

### nova-idea-coordinator cron — added
nova-idea-coordinator: fires 10:05 AM ET, reads novas-choice.json, routes by tier.
- AUTOMATIC → queue to idea_queue.jsonl
- SURFACE/ASK → Telegram to Sen

### coordinator.cjs — built
Reads novas-choice.json, classifies idea by tier, routes accordingly.

### idea_queue.jsonl — created
Persistent queue for AUTOMATIC ideas waiting to be executed by heartbeat.
